// globla
const HOME = "/";

//uesrs
const USERS = "/user"
const LOGIN = "/login";
const LOGOUT = "/logout"

//community
const SUPERVISION = "/supervision"
const COMMUNITY = "/community"

//job
const JOB = "/job"

const routes = {
    home : HOME,
    users : USERS,
    login : LOGIN,
    logout : LOGOUT,
    supervision : SUPERVISION,
    community : COMMUNITY,
    job : JOB
}

export default routes;