extends layouts/main

block content 
    .community
        each item in communities
            +communityBlock( {
                id: item.id,
                title: item.title,
                views: item.description,
                videoFile: item.fileUrl
            })